# Authentication Configuration
# Set these variables to enable HTTP Basic Authentication
# If not set, the application will run without authentication (development only)

AUTH_USERNAME=your-username
AUTH_PASSWORD=your-secure-password